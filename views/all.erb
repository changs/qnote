

<div id="left">
  <a href='#?w=700' class='poplight button' rel='new_post'>
    <span class='add'>New note</span></a>
  <a href='/showall' class='button'>
    <span class='all'>All notes</span></a>
  <a href='#?w=500' class='button poplight' rel="popup_name">
    <span class='tags'>Tags</span></a>
  <a href='/logout' class='button'>
    <span class='logout'>Logout</span></a>
    <a href='#' class='button' onclick="document.search.submit(); return false;">
   <span class='search'>Search</span></a>
  <form action="/search" method="post" accept-charset="utf-8" name="search">
    <input id="in_search" type="text" name="title" size="15">
  </form>
</div>
<div id="right">
  <div id="header">
    Notes from '<%= from %>'
    <div id="logo">
      qnote
    </div>
  </div>
  <div id="notes">
      <% @notes.each do |note| %>
  <div class="note" OnClick="document.location='/note/<%=note.id %>'">
    <div class="date">
      <span class="name"><%= note.date.strftime("%a")%></span> 
      <span class="day"><%= note.date.strftime("%d")%></span> 
    </div>
      <div class="arrow">
        <a href="/note/<%=note.id %>">></a>
      </div>
      <div class="content">
        <span class="title"><%= note.title[0..60] %><%= "…" if note.title.length > 60 %></span>
        <span class="short_content"><%= note.content[0..30] %><%= "…" if note.content.length > 30 %></span>
      </div>
    </div>
    <% end %>
  </div>
</div>
